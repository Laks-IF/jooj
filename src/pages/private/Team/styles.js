import styled from "styled-components";

export const TeamWrapper = styled.div`
  padding: 2rem 0;
  padding-bottom: calc(51px + 2rem);

  background: var(--background);
`;

export const TeamUserLoaderWrapper = styled.div`
  padding: 2rem;

  display: flex;
  justify-content: center;

  opacity: 0.2;
`;

import styled from 'styled-components';

export const RpgWrapper = styled.div`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(120px, 250px));
  grid-gap: 1.5rem;
  justify-content: center;
`;
